<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
  <a class="btn btn-primary" href="<%= locals.authorBaseUrl %>/authors/<%= author.id %>/edit">Edit</a>
  <%- include('../partials/deleteForm', { url: locals.authorBaseUrl + '/authors/' + author.id }) %>
</div>
<% if (books.length > 0) { %>
  <h2 class="page-header">Books by the Author</h2>
  <% include('../partials/bookGrid', { books: books }) %>
<% } %>
